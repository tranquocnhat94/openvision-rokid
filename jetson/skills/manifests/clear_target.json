{
  "id": "openvision.skill.clear_target",
  "version": "0.1.0",
  "name": "clear_target",
  "description": "Clear selected target state and remove target HUD cues.",
  "latency_class": "live",
  "local_first": true,
  "cloud_allowed": false,
  "privacy_level": "low",
  "hud_policy": "clear_target",
  "timeout_ms": 500,
  "local_resources": ["selected_target_state"],
  "activation_phrases_vi": ["bỏ chọn", "xóa mục tiêu", "dừng theo dõi"],
  "activation_phrases_en": ["clear target", "stop tracking"],
  "input_schema": {
    "type": "object",
    "properties": {},
    "required": [],
    "additionalProperties": false
  },
  "result_schema": {
    "type": "object",
    "properties": {
      "cleared": {"type": "boolean"}
    },
    "required": ["cleared"],
    "additionalProperties": false
  },
  "acceptance_tests": ["clears selected target state"],
  "failure_modes": ["none"]
}
