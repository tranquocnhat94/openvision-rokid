<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/candidateTileCard"
    android:layout_width="0dp"
    android:layout_height="wrap_content"
    android:layout_marginEnd="6dp"
    app:cardBackgroundColor="@color/panel_black_soft"
    app:cardCornerRadius="9dp"
    app:cardElevation="0dp"
    app:strokeColor="@color/panel_border"
    app:strokeWidth="0dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_horizontal"
        android:orientation="vertical"
        android:paddingHorizontal="5dp"
        android:paddingTop="5dp"
        android:paddingBottom="6dp">

        <ImageView
            android:id="@+id/candidateThumbImage"
            android:layout_width="54dp"
            android:layout_height="54dp"
            android:adjustViewBounds="false"
            android:background="@drawable/bg_candidate_thumb"
            android:contentDescription="@null"
            android:cropToPadding="true"
            android:padding="2dp"
            android:scaleType="centerCrop"
            android:visibility="gone" />

        <TextView
            android:id="@+id/candidateLabelText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="5dp"
            android:fontFamily="sans-serif-medium"
            android:gravity="center"
            android:maxLines="1"
            android:text="Nguoi 1"
            android:textColor="@color/text_primary"
            android:textSize="9sp" />

        <TextView
            android:id="@+id/candidateSecondaryText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:fontFamily="monospace"
            android:gravity="center"
            android:maxLines="1"
            android:text="Ahead | 0.86"
            android:textColor="@color/text_muted"
            android:textSize="8sp" />
    </LinearLayout>
</com.google.android.material.card.MaterialCardView>
